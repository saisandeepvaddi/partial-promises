{
  "name": "partial-promises",
  "version": "1.0.0",
  "description":
    "Collect promises resolved within a timeout when using Promise.all([p])",
  "main": "dist/index.js",
  "files": ["dist", "README.md", "package.json"],
  "scripts": {
    "dev": "babel src --out-dir dist --watch",
    "compile": "rm -rf dist && babel src --out-dir dist",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/saisandeepvaddi/partial-promises.git"
  },
  "keywords": ["promises", "promise", "async", "await"],
  "author": "Sai Sandeep Vaddi",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/saisandeepvaddi/partial-promises/issues"
  },
  "homepage": "https://github.com/saisandeepvaddi/partial-promises#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-async-to-generator": "^6.24.1",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.2.0",
    "jest": "^21.2.1"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0"
  }
}
